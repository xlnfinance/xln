[package]
name = "xln-native-kernel-napi"
version = "0.0.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "3", features = ["napi8"] }
napi-derive = "3"
xln-native-kernel-bench = { path = ".." }

[profile.release]
lto = "thin"
codegen-units = 1

