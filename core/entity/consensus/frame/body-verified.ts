import type { EntityFrame } from '../../types';

export const markEntityFrameBodyVerified = (_frame: EntityFrame): void => {};

export const entityFrameBodyIsVerified = (_frame: EntityFrame): boolean => false;
