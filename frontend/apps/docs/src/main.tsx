import { mountCandidateSurface } from '../../../packages/ui/src/mount-candidate-surface';

mountCandidateSurface('docs', {
  eyebrow: 'Knowledge surface',
  title: 'Docs, independently built.',
  summary: 'A dedicated React entry for the catalog and reader, ready for behavior-preserving migration slices.',
});
