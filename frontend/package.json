{
	"name": "frontend",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "vite dev",
		"build": "node copy-static-files.js && vite build",
		"build:static": "node copy-static-files.js && vite build && echo 'Built for GitHub Pages deployment'",
		"preview": "vite preview",
		"prepare": "svelte-kit sync || echo ''",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --threshold error && vite build",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch"
	},
	"devDependencies": {
		"@sveltejs/adapter-auto": "^6.0.0",
		"@sveltejs/adapter-static": "^3.0.9",
		"@sveltejs/kit": "^2.22.0",
		"@sveltejs/vite-plugin-svelte": "^6.0.0",
		"marked": "^16.4.0",
		"svelte": "^5.0.0",
		"svelte-check": "^4.0.0",
		"svelte-multiselect": "^11.2.3",
		"typescript": "^5.0.0",
		"vite": "^7.0.4"
	},
	"dependencies": {
		"@types/three": "^0.180.0",
		"lucide-svelte": "^0.544.0",
		"three": "^0.180.0"
	}
}
