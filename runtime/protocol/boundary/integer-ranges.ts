/** Exact EVM ABI integer domains shared by live transitions and storage. */
export const UINT256_MAX = (1n << 256n) - 1n;
export const INT256_MIN = -(1n << 255n);
export const INT256_MAX = (1n << 255n) - 1n;
